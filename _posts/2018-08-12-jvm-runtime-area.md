---
layout: post
title: Java虚拟机的内存结构 - 运行时数据区域
categories: Java虚拟机
tags: [Java,Java虚拟机]
---

JVM虚拟机把它所管理的内存划分为若干个用途不同的内存区域，包括程序计数器、java虚拟机栈、本地方法栈、堆、方法区、运行时常量池、直接内存。

## Java虚拟机运行时数据区域

以下是对各个区域的简单介绍

### 程序计数器

程序计数器是一块较小的内存区域，它是当前线程执行字节码的行号指示器。

线程执行Java方法时记录的是正在执行的虚拟机字节码指令的地址，执行Native方法时记录的是空。

线程私有的一块内存区域。

此内存区域是唯一一个在Java虚拟机规范中没有规定任何OutOfMemoryError情况的区域。


